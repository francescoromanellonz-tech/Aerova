{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "OFFLINE MODE (2026-07-21): the Aerova site is intentionally taken offline while the Vercel project 'build' is kept. The build outputs only a neutral placeholder (scripts/build-offline.mjs) and every response carries noindex. To restore the real site, follow docs/VERCEL_OFFLINE.md.",
  "buildCommand": "node scripts/build-offline.mjs",
  "outputDirectory": "build",
  "trailingSlash": false,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Robots-Tag", "value": "noindex, nofollow, noarchive" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" },
        { "key": "X-Content-Type-Options", "value": "nosniff" }
      ]
    }
  ]
}
